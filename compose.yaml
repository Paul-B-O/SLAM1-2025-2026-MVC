services:
  db:
    image: mysql:8.0.32
    restart: on-failure
    environment:
      MYSQL_DATABASE: 'hotel_gamma'
      MYSQL_USER: 'hotel_gamma'
      MYSQL_PASSWORD: 'hotel_gamma'
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - mysql-data:/var/lib/mysql
  adminer:
    image: adminer
    restart: on-failure
    ports:
      - '8080:8080'
volumes:
  mysql-data:
